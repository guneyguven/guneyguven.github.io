# Apple-class redesign

Implemented on `main` from the approved design brief. The legacy `profile.html` and `gallery.html` URLs redirect to the new Resume and Art pages.

Background video support is intentionally code-only and commented out in `js/apple.js`; there is no frontend video control.